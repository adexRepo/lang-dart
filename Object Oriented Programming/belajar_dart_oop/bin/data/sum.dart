// * Callabel Class

class Sum {
  int first;
  int second;

  Sum(this.first, this.second);

  int call() => first + second;
  //nameing call itu spesial.
}

typedef Total = Sum;
typedef Jumlah = Sum;
